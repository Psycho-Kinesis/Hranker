# Python 3.10+ required (the anthropic and google-genai SDKs both dropped 3.9).
scikit-learn>=1.3
numpy>=1.24
streamlit>=1.32
# Generation providers. Whichever key is set decides which is used at runtime;
# with neither, the app runs in offline fallback mode.
google-genai>=1.0
anthropic>=1.0
# Optional upgrade — uncomment to use real semantic embeddings instead of TF-IDF
# (requires internet access to download model weights on first run)
# sentence-transformers>=2.2
