scikit-learn>=1.3
numpy>=1.24
anthropic>=0.40.0
streamlit>=1.32
# Optional upgrade — uncomment to use real semantic embeddings instead of TF-IDF
# (requires internet access to download model weights on first run)
# sentence-transformers>=2.2
